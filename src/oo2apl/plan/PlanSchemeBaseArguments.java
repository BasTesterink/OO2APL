package oo2apl.plan;
/**
 * Plan scheme base arguments are used by an agent component factory to create a plan 
 * scheme base. The use of a marker interface is to guide developers for specifying what
 * data is needed for building a plan scheme base. This prevents the use of an Object 
 * array as arguments, and hence allows compile-time type checks.
 * 
 * @author Bas Testerink
 */
public interface PlanSchemeBaseArguments {}
